{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "postsetupNO": "react-native link react-native-text-size",
    "setup": "rm -rf ./node_modules && yarn --no-lockfile && node ./ins-text-size",
    "clean": "rm -rf $TMPDIR/react-* && watchman watch-del-all && cd android && ./gradlew clean",
    "clean:watchman": "watchman watch-del-all",
    "test": "node ./ins-text-size"
  },
  "dependencies": {
    "react": "16.4.1",
    "react-native": "0.56.0"
  },
  "devDependencies": {
    "which": "^1.3.1"
  },
  "rnpm": {
    "assets": [
      "./assets/fonts"
    ]
  }
}
